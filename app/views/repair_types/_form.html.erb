<%= form_with(model: repair_type, local: true) do |form| %>
  <% if repair_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repair_type.errors.count, "error") %> prohibited this repair_type from being saved:</h2>

      <ul>
      <% repair_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
