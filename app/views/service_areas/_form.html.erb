<%= form_with(model: service_area, local: true) do |form| %>
  <% if service_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_area.errors.count, "error") %> prohibited this service_area from being saved:</h2>

      <ul>
      <% service_area.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
