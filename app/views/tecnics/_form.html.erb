<%= form_with(model: tecnic, local: true) do |form| %>
  <% if tecnic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tecnic.errors.count, "error") %> prohibited this tecnic from being saved:</h2>

      <ul>
      <% tecnic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
