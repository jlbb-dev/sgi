<%= form_with(model: service_mode, local: true) do |form| %>
  <% if service_mode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service_mode.errors.count, "error") %> prohibited this service_mode from being saved:</h2>

      <ul>
      <% service_mode.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
