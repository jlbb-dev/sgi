<%= form_with(model: repair_history, local: true) do |form| %>
  <% if repair_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(repair_history.errors.count, "error") %> prohibited this repair_history from being saved:</h2>

      <ul>
      <% repair_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :pccode %>
    <%= form.text_field :pccode %>
  </div>

  <div class="field">
    <%= form.label :observation %>
    <%= form.text_area :observation %>
  </div>

  <div class="field">
    <%= form.label :area_id %>
    <%= form.text_field :area_id %>
  </div>

  <div class="field">
    <%= form.label :repair_type_id %>
    <%= form.text_field :repair_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
